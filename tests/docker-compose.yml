version: "3"

services:
   mysql:
    image: mysql:5
    environment:
       MYSQL_ROOT_USER: root
       MYSQL_ROOT_PASSWORD: root
       MYSQL_DATABASE: test
    ports:
       - "3306:3306"
    restart: always

